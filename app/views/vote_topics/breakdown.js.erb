<% if @bd_type == 'all' %>
  $('.simpleSlide-tray').append("<%= escape_javascript(render :partial => 'sex_bd', :locals => {:vote_items => @vote_topic.vote_items, :all => true}) %>");
<% else %>
  $('.simpleSlide-tray').append("<%= escape_javascript(render :partial => 'sex_bd', :locals => {:vote_items => @vote_topic.vote_items, :all => false}) %>");
<% end  %>

simpleSlide({
'set_speed': 300,
'status_width': 20,
'status_color_outside': '<%= Constants::SLIDE_SHOW_PROG_OUTSIDE_COL %>' ,
'status_color_inside': '<%= Constants::SLIDE_SHOW_PROG_INSIDE_COL %>',
'callback': function() {
/* function code */
}
});